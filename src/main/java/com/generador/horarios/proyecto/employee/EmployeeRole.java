package com.generador.horarios.proyecto.employee;

/** Rol de acceso del empleado. MANAGER puede administrar el venue; EMPLOYEE solo su propia semana. */
public enum EmployeeRole {
    MANAGER,
    EMPLOYEE
}
