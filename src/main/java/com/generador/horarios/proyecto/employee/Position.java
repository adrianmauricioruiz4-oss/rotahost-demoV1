package com.generador.horarios.proyecto.employee;

/** Puesto/función que puede desempeñar un empleado. Uno o varios por persona (T5.1). */
public enum Position {
    CAMARERO,
    COCINERO,
    AYUDANTE_COCINA,
    RESPONSABLE_SALA,
    ENCARGADO,
    REPARTIDOR
}
